
defn pgr parse-github-url
defn hb hub browse
defn hpr hub pr
defn hprq hub pull-request

defn ghds github-docs-snapshot
defn ghdss github-docs-snapshot -s

defn hds github-docs-snapshot
defn hdss github-docs-snapshot -s

defn gha "ghh api"
defn ghj 'ghh api -H "Accept: application/vnd.github+json"'
defn ghw github-workflows.py

defn ghow ghh repo view --web
github_open_web_branch() {
    ghh repo view --web -b "$(git current-branch)"
}
export -f github_open_web_branch
defn ghob github_open_web_branch

defn ghrl ghh run list
defn ghrv ghh run view
defn ghrvj ghh run view --job
defn ghrvl ghh run view --log
defn ghrvlj ghh run view --log --job
