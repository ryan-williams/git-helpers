[alias]
        pr      = pull --rebase
        rba     = rebase --abort
        rb      = rebase
        rc      = rebase --continue
        rem     = rebase master
        ri      = rebase -i
        rs      = rebase --skip
	arc = add-rebase-continue.sh
	aurc = add-u-rebase-continue
