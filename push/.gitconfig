[alias]
  cpd = config push.default
  cpdc = config push.default current
  cpdd = config --unset push.default
  cpdu = config push.default upstream
  cfpd = config push.default
  cgpd = config --global push.default
  cgpdc = config --global push.default current
  cgpdu = config --global push.default upstream
  mp = me-push
  mpf = me-push-force
  pm = push-to-master
  pdc = config push.default current
  pdcg = config --global push.default current
  pdu = config push.default upstream
  pdug = config --global push.default upstream
  po = push-to-remote-branch origin
  p = push-x
  pc = config push.default
  pcc = config push.default current
  pcu = config push.default upstream
  pd = push --delete
  pf = push -f
  pfn = push -f -n
  pfo = push -f origin
  pft = push -f --tags
  pftn = push -f --tags -n
  pfu = push -f u
  pgc = config --global push.default
  pgcc = config --global push.default current
  pgcu = config --global push.default upstream
  pnf = push -n -f
  px = push-x
  ph = push-head-to
  phf = push-head-to -f
  phfn = push-head-to -f -n
  phn = push-head-to -n
  pht = push-head-to
  phu = push-head-upstream
  phuf = push-head-upstream -f
  phufn = push-head-upstream -f -n
  phun = push-head-upstream -n
  pn = push -n
  pnt = push -n --tags
  pt = push-x -t
  pto = push --tags
  ptf = push --tags -f
  ptfn = push --tags -f -n
  ptn = push --tags -n
  pon = push -n origin
  pno = push -n origin
  pun = push -n upstream
  pnu = push -n upstream
  put = push --tags upstream
  ptu = push --tags upstream
  pot = push --tags origin
  psu = push --set-upstream
  pto = push --tags origin
  pu = push-user-branch
  pub = push-user-branch
  pbf = push-user-branch -f
  pbn = push-user-branch -n
  pubf = push-user-branch -f
  pubn = push-user-branch -n
  puf = push-user-branch -f
  pun = push-user-branch -n
  spf = safe-push-force
  spfo = safe-push-force origin
  spfu = safe-push-force upstream
