#!/usr/bin/env bash

# Show a pretty `git log` with a graph.

git all-branches | xargs -d $'\n' \
git log \
  --graph \
  --format="%C(auto)%h %C(blue)%ad%C(auto)%d %s" \
  --date=relative \
  --author-date-order \
  "$@"
