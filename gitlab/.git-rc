gitlab_api() {
    if [ -n "$GITLAB_HOST" ]; then
        args=(--hostname "$GITLAB_HOST")
    else
        args=()
    fi
    glab api "${args[@]}" "$@"
}
export -f gitlab_api
defn glapi gitlab_api

alias glob="glab repo view --web"
